
task :default => :test

desc "Run the tests"
task :test do
  sh %{
    $MAGLEV_HOME/bin/testrb \
      -Irails/activesupport/lib \
      -Irails/activemodel/lib \
      -Itest \
      #{FileList['test/*_test.rb']}
  }
end
