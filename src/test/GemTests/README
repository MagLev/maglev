This directory contains tests related to getting various Ruby Gems working
on MagLev.

1. Ensure all required gems are installed by doing:

     $ $MAGLEV_HOME/bin/bundle install

2. Run tests by doing:

     $ rake test

TODO: I shouldn't have to require rubygems in each test file.
