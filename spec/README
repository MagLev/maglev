This directory contains all of the spec related files.

mspec    A git submodule that tracks the mspec framework.
rubyspec A git submodule that tracks the rubyspecs.
tags     The MagLev rubyspec tags.  These are maintained in the git
         repository.
