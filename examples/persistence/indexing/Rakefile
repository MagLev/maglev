task :default => :index

desc "Demo of using unordered collections with indexes."
task :index do
  sh "maglev-ruby unordered_collection.rb"
end

desc "Run the tests"
task :test do
  sh %{ maglev-ruby -Ilib test/names_test.rb }
end
